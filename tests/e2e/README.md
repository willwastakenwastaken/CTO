# E2E tests (placeholder — Playwright)

Golden path (planned): sign in -> complete Sales Profile -> create/open
ABC Roofing -> view Call Strategy -> start simulated call -> advance transcript
-> see price objection + ASK NEXT -> see LISTEN + state updates -> mark a
suggestion Useful -> end session -> view evidence-based review -> apply
Contacted -> Qualified -> refresh and confirm review, note, activity, and stage
persist.

Playwright browsers are intentionally NOT installed in this milestone.
Install them with `npx playwright install` when the e2e milestone lands.
`npm run test:e2e` will not pass until then.
